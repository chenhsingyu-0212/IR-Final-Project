# IR Final Project Requirements
# Install with: pip install -r requirements.txt

# Core ML libraries
torch>=2.0.0
transformers>=4.30.0

# Data processing
tqdm
datasets>=2.0.0

# Web scraping for DuckDuckGo search
requests
beautifulsoup4
ddgs>=9.0.0  # DuckDuckGo search (replaces duckduckgo-search)

# Fine-tuning (Experiment 5)
peft>=0.4.0
accelerate>=0.20.0

# Optional: For GPU support (uncomment if needed)
# torch-audio>=2.0.0  # For audio processing if needed
# faiss-cpu  # Alternative to DuckDuckGo search (not used in current implementation)
# faiss-gpu  # GPU version of FAISS
# sentence-transformers  # For embedding-based retrieval